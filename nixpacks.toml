[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm config set optional=true",
  "npm ci",
  "npm rebuild"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node .output/server/index.mjs"